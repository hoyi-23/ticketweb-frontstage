import { createStore } from 'vuex'
import getProduct from './products/getProduct'
export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    getProduct,
  }
})
